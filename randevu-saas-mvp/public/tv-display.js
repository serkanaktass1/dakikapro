let tvRefreshTimer;
async function tvDisplay(slug){
  clearInterval(tvRefreshTimer); app.innerHTML='<div class="tvWall"><div class="tvLoading">Randevu panosu hazırlanıyor...</div></div>';
  const load=async()=>{try{const response=await fetch(`/data/public/${encodeURIComponent(slug)}/display`,{cache:'no-store'}),data=await response.json();if(!response.ok)throw Error(data.error||'Pano açılamadı.');const now=new Date(),todayCount=data.appointments.length,next=data.appointments.find(item=>item.time>=now.toTimeString().slice(0,5))||data.appointments[0];app.innerHTML=`<main class="tvWall" style="--tv:${data.tenant.color}"><header><div><span>DAKİKAPRO · CANLI RANDEVU PANOSU</span><h1>${esc(data.tenant.name)}</h1><p>${esc(data.tenant.sector||'Bugünün randevu akışı')}</p></div><div class="tvClock"><b>${now.toLocaleTimeString('tr-TR',{hour:'2-digit',minute:'2-digit'})}</b><small>${now.toLocaleDateString('tr-TR',{weekday:'long',day:'numeric',month:'long'})}</small></div></header><section class="tvStats"><article><span>BUGÜN GELECEK</span><strong>${todayCount}</strong><small>planlanan randevu</small></article><article class="tvNext"><span>SIRADAKİ RANDEVU</span><strong>${next?next.time:'—'}</strong><small>${next?`${esc(next.customer)} · ${esc(next.service)}`:'Bugün başka randevu yok'}</small></article><article><span>EKRAN DURUMU</span><strong>CANLI</strong><small>30 saniyede bir yenilenir</small></article></section><section class="tvList"><div class="tvListHead"><h2>Bugünün randevuları</h2><span>SAAT · MİSAFİR · HİZMET · UZMAN</span></div>${data.appointments.map(item=>`<div class="tvRow ${next&&item.time===next.time?'isNext':''}"><b>${item.time}</b><strong>${esc(item.customer)}</strong><span>${esc(item.service)}</span><span>${esc(item.staff)}</span></div>`).join('')||'<div class="tvEmpty">Bugün için planlanmış randevu yok.</div>'}</section><footer>Bu ekran işletmeye özeldir · Müşteri adları gizlilik için kısaltılmış gösterilir.</footer></main>`;}catch(error){app.innerHTML=`<div class="tvWall"><div class="tvError"><h1>Randevu panosu açılamadı</h1><p>${esc(error.message)}</p><a href="/" class="btn">Ana sayfaya dön</a></div></div>`;}};await load();tvRefreshTimer=setInterval(load,30000);
}
setInterval(()=>{if(location.hash==='#business'&&ownerSession){const main=document.querySelector('.main');const tenant=tdata().t;if(!main||main.querySelector('[data-tv-link]')||!tenant.tvDisplayEnabled)return;main.insertAdjacentHTML('beforeend',`<section class="card tvOwnerCard" data-tv-link><div><h2>TV ekranı</h2><p class="sub">Televizyonunuzda bugünün randevularını, sıradaki misafiri ve yoğunluğu canlı gösterin.</p></div><a class="btn" target="_blank" href="/#display/${encodeURIComponent(tenant.slug)}">TV ekranını aç</a></section>`);}},500);
