let tvSoundSignature='';
function shortTvSound(){try{const Ctx=window.AudioContext||window.webkitAudioContext,ctx=new Ctx(),osc=ctx.createOscillator(),gain=ctx.createGain();osc.frequency.value=880;gain.gain.setValueAtTime(.05,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(.001,ctx.currentTime+.15);osc.connect(gain);gain.connect(ctx.destination);osc.start();osc.stop(ctx.currentTime+.16)}catch(_){}}
setInterval(async()=>{if(!location.hash.startsWith('#display/'))return;try{const slug=decodeURIComponent(location.hash.slice(9)),r=await fetch(`/data/public/${encodeURIComponent(slug)}/display`,{cache:'no-store'}),d=await r.json();if(!r.ok)return;const signature=d.appointments.filter(item=>item.status==='confirmed').map(item=>item.id).join(',');if(tvSoundSignature&&signature!==tvSoundSignature)shortTvSound();tvSoundSignature=signature;document.querySelectorAll('.tvRow').forEach(row=>{if(row.textContent.includes('İptal'))row.classList.add('isCancelled')});}catch(_){}} ,30000);
const tvStyle=document.createElement('style');tvStyle.textContent='.tvRow.isCancelled{opacity:.55;background:#fff1f2;text-decoration:line-through}.tvRow.isCancelled span:last-child{color:#be123c;font-weight:800}';document.head.appendChild(tvStyle);
